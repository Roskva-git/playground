# Startup Trends – SQL Aggregation

SQL exercise from Codecademy analyzing a dataset of startup companies.

Concepts practiced:
- aggregate functions (AVG)
- grouping results with GROUP BY
- filtering aggregated results using HAVING

The query calculates the average number of employees for startups
in each location and returns only the locations where the average
company size exceeds 500 employees.
